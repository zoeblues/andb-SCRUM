
  # Beauty Salon Add Service

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.

  This a prototype management service for beauty salon.
